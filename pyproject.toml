[tool.black]
target-version = ["py310"]
extend-exclude = "(^app/static/|^node_modules/|^model/|^data/)"

[tool.ruff]
target-version = "py310"
extend-exclude = ["app/static/", "node_modules/", "model/", "data/"]

[tool.ruff.lint]
select = ["E", "F"]

[tool.ruff.format]
indent-style = "tab"
